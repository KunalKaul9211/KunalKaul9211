apiVersion: apps/v1
kind: Deployment
metadata:
  name: monkmedia-redis
  namespace: monkmedia-nrcs
  labels:
    app: monkmedia-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monkmedia-redis
  template:
    metadata:
      labels:
        app: monkmedia-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        volumeMounts:
        - name: redis-storage
          mountPath: /data
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: redis-storage
        persistentVolumeClaim:
          claimName: redis-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: monkmedia-redis
  namespace: monkmedia-nrcs
  labels:
    app: monkmedia-redis
spec:
  selector:
    app: monkmedia-redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  namespace: monkmedia-nrcs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi